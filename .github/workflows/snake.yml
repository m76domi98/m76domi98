name: generate snake animation

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: generate snake grid
        uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/snake.svg?color_snake=ff4da6&color_dots=#f5edff,#e0c3fc,#ffb6c1,#ff9ec4,#ff4da6
            dist/snake-dark.svg?color_snake=ff4da6&color_dots=#2d1b3d,#6b2d5c,#a855f7,#ff69b4,#ff4da6
            dist/snake.gif?color_snake=ff4da6&color_dots=#ffe4ec,#ffb6c1,#ff9ec4,#ff6fb0,#ff4da6

      - name: push to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
